# Maris4u

This repository contains a starter e-commerce project with a Node/Express backend and a Vite + React frontend.

- backend/: Express API
- frontend/: Vite + React app

Run backend:
- cd backend
- npm install
- cp .env.example .env
- fill .env and run npm run dev

Run frontend:
- cd frontend
- npm install
- npm run dev
